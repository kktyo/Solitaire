# docker compose 用。このファイルはコミットしてよい。
# 実運用の値は code/docker/.env に書く（.gitignore 済み）。
DB_PASSWORD=changeme-local-only
JWT_ACCESS_SECRET=changeme-must-be-at-least-32-characters
JWT_REFRESH_SECRET=changeme-must-be-at-least-32-characters
